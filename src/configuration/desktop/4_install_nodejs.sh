# INFO - s: Silent mode, L: follow redirects, E: security policy to preserve environment variables, 
# -: to execute pipelined bash script

# Node.js: Runtime Environment
curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash -
sudo apt install nodejs
read
